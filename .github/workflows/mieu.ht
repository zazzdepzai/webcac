<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sakura Premium | Dashboard</title>
    <link href="https://fonts.googleapis.com/css2?family=Quicksand:wght@500;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --ios-blue: #007AFF;
            --ios-green: #34C759;
            --premium-gold: #FFD700;
            --pink-bg: #ffafbd;
            --glass: rgba(255, 255, 255, 0.8);
        }

        body {
            margin: 0;
            font-family: 'Quicksand', sans-serif;
            background: url('https://w0.peakpx.com/wallpaper/950/25/HD-wallpaper-anime-scenery-road-clouds-sky-sunset-anime-landscape.jpg') no-repeat center center fixed;
            background-size: cover;
            display: flex; justify-content: center; min-height: 100vh; padding: 20px 0;
            color: #1c1c1e;
        }

        /* Khung điện thoại iOS */
        .app-container {
            width: 95%; max-width: 420px;
            background: var(--glass);
            backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
            border-radius: 40px; border: 4px solid rgba(255,255,255,0.5);
            display: flex; flex-direction: column; overflow: hidden;
            box-shadow: 0 25px 50px rgba(0,0,0,0.4);
        }

        /* Header */
        .header { padding: 25px 20px; text-align: center; background: rgba(255,255,255,0.2); }
        .anime-avatar { width: 80px; height: 80px; border-radius: 50%; border: 3px solid white; box-shadow: 0 5px 15px rgba(0,0,0,0.2); }

        /* Nội dung */
        .content { padding: 20px; flex-grow: 1; overflow-y: auto; max-height: 500px; }
        .tab-panel { display: none; animation: fadeIn 0.4s ease; }
        .active { display: block; }

        @keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

        /* Card Style */
        .card {
            background: white; border-radius: 20px; padding: 15px; margin-bottom: 15px;
            box-shadow: 0 4px 10px rgba(0,0,0,0.05);
        }

        /* Buttons */
        .btn {
            width: 100%; padding: 14px; border-radius: 15px; border: none;
            font-weight: 700; cursor: pointer; transition: 0.3s; margin-top: 10px;
        }
        .btn-blue { background: var(--ios-blue); color: white; }
        .btn-gold { background: linear-gradient(45deg, #FFD700, #FFA500); color: black; }
        .btn-outline { background: transparent; border: 2px solid var(--ios-blue); color: var(--ios-blue); }

        /* Input */
        input, textarea {
            width: 100%; padding: 12px; border-radius: 12px; border: 1px solid #eee;
            background: #f9f9f9; outline: none; box-sizing: border-box; font-family: sans-serif;
        }

        /* Navigation Bar */
        .nav-bar {
            display: flex; justify-content: space-around; background: white;
            padding: 15px 0; border-top: 1px solid #eee;
        }
        .nav-item { cursor: pointer; font-size: 11px; font-weight: 700; color: #8e8e93; text-align: center; }
        .nav-item.active-nav { color: var(--ios-blue); }
        .nav-item i { display: block; font-size: 20px; margin-bottom: 4px; }
    </style>
</head>
<body>

<div class="app-container">
    <div class="header">
        <img src="https://i.pinimg.com/originals/ba/92/7f/ba927ff34cd961ce2cff84d97304040a.png" class="anime-avatar">
        <h2 style="margin: 10px 0 0 0;">Sakura Premium</h2>
        <span style="font-size: 12px; opacity: 0.7;">Version 2.0.1 • Modded Hub</span>
    </div>

    <div class="content">
        <div id="getkey" class="tab-panel active">
            <div class="card">
                <h3>🔑 Quy trình lấy Key</h3>
                <p style="font-size: 13px; color: #666;">Hoàn thành các bước sau để nhận mã kích hoạt 24h.</p>
                <div style="font-size: 14px; line-height: 2;">
                    1️⃣ Bấm <b>"Tạo Link GetKey"</b><br>
                    2️⃣ Vượt qua Work.ink (2 bước)<br>
                    3️⃣ Copy mã dán vào tab <b>Loader</b>
                </div>
                <button class="btn btn-blue" onclick="generateKey()">Tạo Link GetKey (Work.ink)</button>
            </div>
        </div>

        <div id="loader" class="tab-panel">
            <div class="card">
                <h3>🚀 Script Loader</h3>
                <input type="text" id="keyInput" placeholder="Nhập Key của bạn...">
                <button class="btn btn-blue" onclick="checkKey()">Kích hoạt hệ thống</button>
            </div>
            <div id="scriptResult" class="card" style="display: none;">
                <p style="font-size: 13px; font-weight: bold;">Mã Script VIP:</p>
                <textarea id="scriptCode" readonly>_G.SakuraMode = "Premium"; loadstring(game:HttpGet("https://api.sakura.io/lib.lua"))()</textarea>
                <button class="btn btn-outline" onclick="copyCode()">Copy Script</button>
            </div>
        </div>

        <div id="store" class="tab-panel">
            <div class="card">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <b>💎 Modded Script VIP</b>
                    <span style="color: #34C759; font-weight: bold;">$5.00</span>
                </div>
                <p style="font-size: 12px; color: #666;">Antiban 100%, No Key, Full tính năng độc quyền.</p>
                <button class="btn btn-gold" onclick="window.open('https://t.me/your_admin')">MUA QUA TELEGRAM</button>
            </div>
            <div class="card">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <b>🔥 Auto Farm Mod</b>
                    <span style="color: #34C759; font-weight: bold;">$2.50</span>
                </div>
                <button class="btn btn-gold" onclick="window.open('https://t.me/your_admin')">MUA QUA TELEGRAM</button>
            </div>
        </div>

        <div id="docs" class="tab-panel">
            <div class="card">
                <h3>📚 Hướng dẫn sử dụng</h3>
                <p style="font-size: 13px;">- <b>Lỗi trắng màn hình:</b> Xóa cache trình duyệt.<br>- <b>Lỗi không nhận key:</b> Đảm bảo không dùng VPN khi get key.<br>- <b>Thời gian Key:</b> 24h kể từ khi kích hoạt.</p>
            </div>
        </div>
    </div>

    <div class="nav-bar">
        <div class="nav-item active-nav" onclick="showTab('getkey', this)">🔑<br>GetKey</div>
        <div class="nav-item" onclick="showTab('loader', this)">🚀<br>Loader</div>
        <div class="nav-item" onclick="showTab('store', this)">💎<br>Store</div>
        <div class="nav-item" onclick="showTab('docs', this)">📚<br>Docs</div>
    </div>
</div>

<script>
    function showTab(tabId, el) {
        document.querySelectorAll('.tab-panel').forEach(p => p.classList.remove('active'));
        document.querySelectorAll('.nav-item').forEach(n => n.classList.remove('active-nav'));
        document.getElementById(tabId).classList.add('active');
        el.classList.add('active-nav');
    }

    function generateKey() {
        alert("Đang tạo link Work.ink... Vui lòng chờ!");
        window.open("https://work.ink/your-link-here", "_blank");
    }

    function checkKey() {
        const k = document.getElementById('keyInput').value;
        if(k === "SAKURA_FREE") { // Key mẫu
            alert("Kích hoạt thành công!");
            document.getElementById('scriptResult').style.display = "block";
        } else {
            alert("Key sai hoặc đã hết hạn!");
        }
    }

    function copyCode() {
        const text = document.getElementById('scriptCode');
        text.select();
        document.execCommand('copy');
        alert("Đã copy script!");
    }
</script>

</body>
</html>
